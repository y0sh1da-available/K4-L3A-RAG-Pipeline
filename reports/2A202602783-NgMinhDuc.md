# Individual contribution report

## Thông tin

- Họ và tên: Nguyễn Minh Đức
- Mã học viên: 2A202602783
- Nhóm: y0sh1da (Chủ đề: Sách và Tri thức)
- Repository/branch: `https://github.com/y0sh1da-available/K4-L3A-RAG-Pipeline` / `feature/duc` -> `develop`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 8 – PageIndex fallback | Tích hợp truy xuất qua PageIndex, lưu cache document ID, lấy evidence từ block/page, giữ metadata nguồn và xử lý lỗi provider. Chưa xác nhận chạy tích hợp với metadata của toàn bộ corpus. | `src/task8_pageindex_vectorless.py` | Partial |
| Task 9 – Retrieval pipeline | Kết nối semantic search, BM25 và RRF; dùng cosine gốc để quyết định fallback; giữ kết quả hybrid khi PageIndex lỗi; chuẩn hóa và loại trùng kết quả. | `src/task9_retrieval_pipeline.py` | Done |
| Task 10 – Generation | Xây dựng context từ các chunk, tích hợp gọi LLM, kiểm tra citation, trả safe refusal và ánh xạ citation về đúng phần tử trong sources. | `src/task10_generation.py` | Done |
| Giao diện chatbot | Kết nối Task 10 với Streamlit; hiển thị answer, nguồn, score và retrieval method; lưu lịch sử trong session và xử lý lỗi. Đã khởi chạy app. | `app.py` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Dùng cosine score cao nhất của dense search trước khi fuse để quyết định gọi PageIndex.  
   **Lý do/evidence:** Cosine và RRF khác thang đo; lấy score trước fusion tránh ảnh hưởng nếu reranker sửa dữ liệu đầu vào. Khi PageIndex lỗi, pipeline vẫn trả hybrid results.  
   **Trade-off:** Threshold cần hiệu chỉnh theo corpus; score thấp không đồng nghĩa nguồn không có giá trị.

2. **Quyết định:** Kiểm tra citation bằng code và ánh xạ lại số nguồn sau khi reorder context.  
   **Lý do/evidence:** Bảo đảm `[Document N]` trong câu trả lời cuối khớp `sources[N-1]`, đồng thời sources vẫn được sắp theo score giảm dần.  
   **Trade-off:** Kiểm tra được citation thiếu hoặc vượt phạm vi, nhưng chưa xác minh nguồn có thực sự hỗ trợ từng khẳng định.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: dữ liệu mock cho dense score cao/thấp, PageIndex lỗi, chunk rỗng/trùng và citation hợp lệ/không hợp lệ.
- Kết quả trước/sau nếu có: bản sửa đã qua 11 kiểm tra mock trong quá trình hỗ trợ phát triển; ba hàm test gốc về reorder, fuse một lần và fallback lỗi cũng chạy độc lập thành công. Chưa xác nhận toàn bộ test suite hoặc kết quả evaluation trên corpus thật.
- Lỗi đã phát hiện và cách xử lý: `retrieval_source` có thể ngoài enum → chuẩn hóa giá trị; citation có thể lệch sau reorder → ánh xạ theo ID; cache PageIndex thiếu metadata → yêu cầu metadata gốc và bỏ qua entry chưa đủ thông tin.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: guardrail citation chưa phát hiện câu trả lời có nguồn hợp lệ nhưng nội dung không được nguồn hỗ trợ.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: bổ sung bước đối chiếu từng khẳng định với nguồn trích dẫn và đánh giá trên golden Q&A của nhóm.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 20/09/2026
- Tên thành viên: Nguyễn Minh Đức